
supreme.symbols <- function() {

  # Symbols to draw a Unicode tree
  # https://atom.io/packages/ascii-tree
  arrow <- list(
    H = "─",
    V = "│",
    Vup.Vdown.Hright = "├",
    Vup.Hright = "└"
  )

  list(
    arrow = arrow
  )
}
